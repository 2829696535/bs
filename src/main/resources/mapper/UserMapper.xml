<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.example.user.mapper.UserMapper">
    <insert id="insertUser" parameterType="org.example.pojo.User" useGeneratedKeys="true" keyProperty="id">
        insert into user (id,username,password,role,create_time)
        values (#{id},#{username}, #{password}, #{role},#{createTime})
    </insert>
    <update id="updateUser" parameterType="org.example.pojo.User" useGeneratedKeys="true" keyProperty="id">
        UPDATE user
        SET username = #{username}, password = #{password}, role = #{role}
        WHERE id = #{id}
    </update>
    <delete id="deleteUser" parameterType="org.example.pojo.User">
              delete from user where usesname = #{usesname} ;
         </delete>


</mapper>
